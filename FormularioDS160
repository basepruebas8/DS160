<!doctype html>
<html lang="es-MX">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover" />
  <meta name="color-scheme" content="light dark" />
  <meta name="description" content="Sitio estático publicado en Deno Deploy" />
  <title>Mi sitio — Deno Deploy</title>
  <style>
    :root{
      --bg:#0b1220; --ink:#e8eefc; --muted:#9fb0d0; --card:#121a2b; --accent:#22d3ee;
    }
    @media (prefers-color-scheme: light){
      :root{ --bg:#f6f7fb; --ink:#0e1320; --muted:#3a4a6a; --card:#ffffff; --accent:#06b6d4; }
    }
    *{ box-sizing:border-box }
    html,body{ height:100% }
    body{
      margin:0;
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji","Segoe UI Emoji";
      background:
        radial-gradient(1200px 600px at 10% -10%, rgba(34,211,238,.15), transparent 60%),
        radial-gradient(900px 500px at 110% 10%, rgba(34,211,238,.12), transparent 60%),
        var(--bg);
      color:var(--ink);
      display:grid;
      place-items:center;
      padding:24px;
    }
    main{
      width:min(720px,100%);
      background:var(--card);
      border:1px solid rgba(255,255,255,.06);
      border-radius:24px;
      padding:32px;
      box-shadow:0 10px 30px rgba(0,0,0,.2);
    }
    h1{ margin:0 0 8px; font-size:clamp(22px,3vw,36px) }
    p{ margin:0 0 16px; line-height:1.55; color:var(--muted) }
    .row{ display:flex; gap:12px; align-items:center; margin-bottom:12px }
    .dot{ width:10px; height:10px; border-radius:999px; background:#22c55e; box-shadow:0 0 0 3px rgba(34,197,94,.2) }
    .btn{
      display:inline-block; padding:10px 14px; border-radius:12px;
      border:1px solid rgba(255,255,255,.08); background:transparent; color:var(--ink);
      text-decoration:none; transition:.2s transform, .2s box-shadow; user-select:none;
    }
    .btn:hover{ transform:translateY(-1px); box-shadow:0 6px 14px rgba(0,0,0,.15) }
    code{
      background:rgba(255,255,255,.06); border:1px solid rgba(255,255,255,.08);
      padding:2px 6px; border-radius:8px;
    }
    footer{ margin-top:20px; font-size:.9rem; color:var(--muted) }
  </style>
</head>
<body>
  <main>
    <div class="row">
      <span class="dot" aria-hidden="true"></span>
      <strong>Sitio activo</strong>
    </div>

    <h1>¡Hola! Tu sitio está desplegado</h1>
    <p>Este es <code>index.html</code>. Si lo ves en tu URL <em>*.deno.dev</em>, la publicación funcionó.</p>

    <p>Para editar, modifica este archivo y haz <code>git push</code> a tu rama (por ejemplo <code>main</code>).</p>

    <div class="row" style="margin-top:16px;">
      <a class="btn" href="./" rel="nofollow">Raíz</a>
      <a class="btn" href="./idex.html">Probar <code>idex.html</code> (si existe)</a>
    </div>

    <footer>
      <p>Archivos estáticos (CSS/JS/imagenes) deben estar en el repo y referenciarse con rutas relativas, p. ej. <code>./style.css</code> o <code>./img/logo.png</code>.</p>
    </footer>
  </main>

  <script>
    // Comprobación en consola
    console.log("index.html servido correctamente.");
  </script>
</body>
</html>
